{
    "title": "Schema for an individual OT-2 protocol run step",
    "type": "object",
    "properties": {
        "step_type": {
            "const": "ot2_protocol"
        },
        "step_name": {
            "type": "string"
        },
        "ot2_name": {
            "type": "string"
        },
        "filepath": {
            "type": "string"
        },
        "prereq_steps": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "metadata": {
            "$ref": "../metadata.json"
        }
    },
    "required": [
        "step_type",
        "step_name",
        "ot2_name",
        "filepath"
    ],
    "additionalProperties": false
}